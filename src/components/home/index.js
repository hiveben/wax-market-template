import React from 'react';
import DefaultHeader from "../defaultheader/DefaultHeader";

const Home = () => {
    return (
        <div className={"Page"}>
            <DefaultHeader/>
        </div>
    );
};

export default Home;
